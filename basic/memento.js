/*example to use cache for Fibbonacci */var memorize_fib = function(){    var cache = {};        return function(n){        if(cache[n]){            return cache[n];        }        else {            if(n<2){                return 1;            }            var data = memorize_fib(n - 1) + memorize_fib(n - 2);            cache[n] = data;            return data;        }    }}();/*example to use cache for pagging */var Page = function(){   var page = 1,         cache = {},         data;         return function(page){        if (cache[page]){            data =  cache[page];            render(data);        }else{            Ajax.send("cgi.xx.com/xxx", function(data){                cache[page] = data;                render(data);            })        }    }}();